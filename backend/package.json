{
  "name": "padelhub-functions",
  "version": "0.1.0",
  "private": true,
  "description": "PadelHub API — Firebase Cloud Functions in front of Cloud SQL (PostgreSQL).",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "typecheck": "tsc --noEmit",
    "migrate": "node lib/scripts/migrate.js"
  },
  "dependencies": {
    "@google-cloud/cloud-sql-connector": "^1.3.4",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.1",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.17.6",
    "@types/pg": "^8.11.10",
    "typescript": "^5.6.3"
  }
}
